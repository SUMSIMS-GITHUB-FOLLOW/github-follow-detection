import { FollowerCard } from '@organisms';
import { FollowersContainer } from './styles';

const index = () => {
  return (
    <FollowersContainer>
      <FollowerCard />
    </FollowersContainer>
  );
};

export default index;
